// O'zgaruvchilarni e'lon qilish (yosh va pul miqdori)
let yoshi = 18;         // Foydalanuvchi yoshini shu yerga yozing
let puli = 25000;       // Foydalanuvchining pulini so'mda yozing

// Shartlarni tekshirish (yoshi 16 dan katta VA puli 20 000 so'mdan ko'p)
if (yoshi > 16 && puli > 20000) {
    console.log("Kinoga kirishingiz mumkin");
} else {
    console.log("Kirish taqiqlanadi");
}
